import { StyleSheet } from 'react-native';
import { secondryColor } from '../../style/colors';

export const genreStyling = StyleSheet.create({
    genre: {
        backgroundColor:secondryColor,
        margin:2,
        paddingVertical:2,
        paddingHorizontal:10,
        borderRadius:50,
    },

})